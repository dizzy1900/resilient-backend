# ===================================================================
# ADAPTMETRIC BACKEND - STRICT REQUIREMENTS
# Generated: 2026-02-13
# Python: 3.9+
# ===================================================================
# This file contains pinned versions to ensure reproducible builds
# All versions tested and verified compatible with geospatial stack
# ===================================================================

# Web Framework (FastAPI)
click==8.1.8
fastapi==0.109.0
uvicorn==0.27.0
python-multipart==0.0.9

# FastAPI/ASGI dependency pins (kept explicit to support --no-deps installs)
starlette==0.35.1
anyio==3.7.1
sniffio==1.3.1
h11==0.14.0

# Production WSGI Server
gunicorn==23.0.0

# Google Earth Engine (Geospatial Core)
earthengine-api==1.6.15

# Google Cloud Dependencies (required by earthengine-api)
google-api-python-client==2.188.0
google-api-core==2.29.0
google-auth==2.47.0
google-auth-httplib2==0.3.0
google-cloud-storage==3.8.0
google-cloud-core==2.5.0
google-resumable-media==2.8.0
google-crc32c==1.8.0
googleapis-common-protos==1.72.0
httplib2==0.31.2

# Data Science Stack (Geospatial Compatible)
numpy==2.0.2           # Core numerical computing
pandas==2.3.3          # Data manipulation and analysis
scipy==1.13.1          # Scientific computing

# Geospatial Libraries
shapely==2.0.6         # Geometric operations and spatial analysis
geopandas==1.0.1       # Geospatial data manipulation
fiona==1.10.1          # Geospatial file I/O (required by geopandas)
pyproj==3.6.1          # Cartographic projections (required by geopandas)

# Machine Learning
scikit-learn==1.8.0    # ML algorithms and surrogate models (must match pickled models)
joblib==1.5.3          # Parallel computing and model persistence
threadpoolctl==3.6.0   # Thread pool control for numpy/scipy

# Task Queue (Background Jobs)
celery==5.4.0          # Distributed task queue for batch processing
redis==5.2.1           # Redis client (Celery broker + backend)

# Graph Analysis
networkx==3.2.1        # Graph algorithms for supply chain cascading failures

# HTTP & Networking
requests==2.32.5
urllib3==2.6.3
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11

# Database (Supabase Client)
supabase==2.12.0       # Supabase Python client for PostgreSQL
httpx==0.27.2          # Async HTTP client (required by supabase <0.29,>=0.26)
postgrest==0.19.3      # PostgREST client (required by supabase <0.20,>=0.19)
realtime==2.7.0        # Realtime client (required by supabase <3.0.0,>=2.0.0)
storage3==0.11.0       # Supabase storage client (required <0.12,>=0.10)
supafunc==0.9.4        # Supabase functions client (required <0.10,>=0.9)
gotrue==2.11.0         # Supabase auth client (required <3.0.0,>=2.11.0)

# Data Validation
pydantic>=2.11.7        # Data validation
email-validator==2.2.0  # Required by Pydantic EmailStr

# Analytics & Monitoring
posthog==6.9.3         # Product analytics
backoff==2.2.1         # Retry logic with exponential backoff

# Environment & Configuration
python-dotenv==1.2.1   # Load environment variables from .env

# Utilities
python-dateutil==2.9.0.post0
pytz==2025.2           # Timezone handling (pandas dependency)
six==1.17.0            # Python 2/3 compatibility
packaging==26.0        # Version parsing and comparison
typing_extensions==4.15.0

# Protocol Buffers (Google APIs)
protobuf==6.33.4
proto-plus==1.27.0

# Cryptography & Security
pyasn1==0.6.2
pyasn1_modules==0.4.2
rsa==4.9.1

# Auth Engine (JWT + password hashing + async DB)
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
PyJWT==2.12.1
SQLAlchemy==2.0.36
aiosqlite==0.20.0
asyncpg==0.30.0            # Async PostgreSQL driver for SQLAlchemy (production)
greenlet==3.0.3          # Required for async SQLAlchemy operations

# URI Templates (Google APIs)
uritemplate==4.2.0
pyparsing==3.3.2

# PDF Generation
weasyprint==65.0           # HTML-to-PDF engine for server-side report generation
Jinja2==3.1.6              # HTML template engine for PDF reports
MarkupSafe==3.0.2          # Required by Jinja2 for safe HTML escaping

# System Info
distro==1.9.0

# ===================================================================
# COMPATIBILITY NOTES:
# ===================================================================
# - numpy 2.4.1 is compatible with pandas 3.0.0 and earthengine-api
# - scikit-learn 1.8.0 requires numpy>=1.26.0 and scipy>=1.6.0
# - pandas 3.0.0 requires numpy>=1.26.0, <3.0
# - All Google Cloud libraries are pinned to tested versions
# - earthengine-api 1.7.10 is the latest stable with numpy 2.x support
#
# INSTALLATION:
# pip install -r requirements.txt --no-deps
# (use --no-deps to enforce these exact versions without resolver)
#
# For cloud deployment (Railway, Render, etc.):
# These versions have been tested and verified compatible
# ===================================================================
